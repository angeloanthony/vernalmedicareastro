PLACE YOUR IMAGES HERE.

This folder maps to the site root, so files here are served at /pictures/...
exactly as the original site expected (e.g. pictures/Rocco.webp -> /pictures/Rocco.webp).

Before deploying, copy your original `pictures/` folder contents into this folder.
All pages reference images via relative `pictures/...` paths, unchanged from the
original site, so no code changes are needed — just drop the images in.
