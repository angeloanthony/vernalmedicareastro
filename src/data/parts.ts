// ---------------------------------------------------------------------------
// src/data/parts.ts  —  Medicare Parts A/B/C/D reference  (SCAFFOLD)
//
// The structure of Medicare itself. Seeded in a later phase; the shape is fixed
// now so pages can rely on it. Type: types/MedicarePlan.ts (MedicarePart).
// ---------------------------------------------------------------------------

import type { MedicarePart } from '../types/MedicarePlan';

// TODO (Phase 4): seed Parts A, B, C, D with plain-English "covers" summaries.
export const PARTS: MedicarePart[] = [];
